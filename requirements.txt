# Karaoke Studio dependencies
# Install with: pip install -r requirements.txt
# Also requires a system ffmpeg build with drawtext, libass and libfreetype
# (e.g. `brew install ffmpeg` on macOS).

librosa>=0.10
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pillow>=10.0
pydub>=0.25
openai-whisper>=20231117
